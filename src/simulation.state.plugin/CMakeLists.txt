set(source
StateManagerPlugin.h
StateManagerPlugin.cpp
StateView.h
StateView.cpp
../include/simulation.state.plugin.h

)
set(moc
# StateView.h 
#StateManagerPlugin.h
)
set(ui
 stateview.ui
)

source_group("" FILES ${source})

find_package(Qt4 COMPONENTS QtCore QtGui QtOpenGL qtmain REQUIRED)
include(${QT_USE_FILE})
include_directories(${CMAKE_CURRENT_BINARY_DIR})
QT4_WRAP_CPP(mocFiles ${moc})
QT4_WRAP_UI(uiFiles ${ui})

source_group("" FILES ${source})
source_group("autogenerated" FILES ${mocFiles} ${uiFiles})
source_group("ui" FILES ${ui})

add_library(simulation.state.plugin ${source} ${mocFiles} ${uiFiles} CMakeLists.txt)


target_link_libraries(simulation.state.plugin
  application.qt
  core
  core.hub
  simulation.state
  ${QT_LIBRARIES})
	   
