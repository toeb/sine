set(source
 ViewportPlugin.h
 ViewportPlugin.cpp
 GlWidget.h
 GlWidget.cpp
 GlWidgetPluginWindow.h
 GlWidgetPluginWindow.cpp
)
set(moc
 ViewportPlugin.h
 GlWidgetPluginWindow.h
 GlWidget.h
)
set(ui
glviewportwidget.ui
)

source_group("" FILES ${source})

find_package(Qt4 COMPONENTS QtCore QtGui QtOpenGL qtmain REQUIRED)
include(${QT_USE_FILE})
include_directories(${CMAKE_CURRENT_BINARY_DIR})
QT4_WRAP_CPP(mocFiles ${moc})
QT4_WRAP_UI(uiFiles ${ui})

source_group("" FILES ${source})
source_group("autogenerated" FILES ${mocFiles} ${uiFiles})
source_group("ui" FILES ${ui})

add_library(visualization.opengl.qt ${source} ${mocFiles} ${uiFiles} CMakeLists.txt)


target_link_libraries(visualization.opengl.qt
  core
  core.hub
  application.qt
  ${QT_LIBRARIES})
	   
